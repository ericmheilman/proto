syntax = "proto3";
package helium.txns;

import "nanopb.proto";

message txn_payment_v1 {
    bytes payer = 1 [(nanopb).max_size = 33];
    bytes payee = 2 [(nanopb).max_size = 33];
    uint64 amount = 3;
    uint64 fee = 4;
    uint64 nonce = 5;
    bytes signature = 6 [(nanopb).max_size = 64];
}